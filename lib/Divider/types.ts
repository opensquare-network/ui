export type DividerProps = {
  /**
   * @deprecated use `mx` instead
   */
  gap?: number;

  /**
   * @default 20
   * @description css margin top and bottom
   */
  my?: number;

  mt?: number;

  mb?: number;
};
